lua press("shoot_half")
lua repeat sleep(10) until get_shooting() == true
lua release("shoot_half")
lua 'repeat sleep(10) until get_shooting() == false'
lua set_aflock(1)
lua set_aelock(1)
lua 'repeat n = n - 1; ecnt=get_exp_count(); press("shoot_full_only"); repeat sleep(10) until (get_exp_count()~=ecnt);	release("shoot_full_only");	until n < 1'
lua set_aelock(0)
lua set_aflock(0)

