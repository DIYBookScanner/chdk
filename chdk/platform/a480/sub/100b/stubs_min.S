#include "stubs_asm.h"

DEF(canon_shoot_menu_active,            0xFFC00414)     //it works, but in AFL mode OSD disappers
DEF(recreview_hold,                     0xFFC00414)     // not found
DEF(enabled_refresh_physical_screen,    0x73A0 + 0x30)  //RefreshPhysicalScreen
